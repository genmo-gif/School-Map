{
  "name": "dgeschool",
  "version": "1.0.0",
  "description": "학교 검색 지도 웹앱 (NEIS + OpenStreetMap)",
  "scripts": {
    "test": "jest --testTimeout=15000",
    "dev": "vercel dev"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "vercel": "^44.0.0",
    "playwright": "^1.61.1"
  }
}
